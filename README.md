# ELK-practice

Place to keep track of my learning progress on ELK stack

## How to Operate

You can use the `Makefile` on root directory to easily navigate through this project

## Security Lessons

### Steps to setup the project for it

1. Provide environmental values for `ELASTICSEARCH_USERNAME` and `ELASTIC_PASSWORD` in the `.env` file. If it doesn't exist, use `.env.example` as reference and create the file.
